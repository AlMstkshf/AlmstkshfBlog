// postcss.config.js
export default {
  plugins: {
    tailwindcss: {
      // if your Tailwind config isn’t in the default location,
      // you can explicitly point to it here:
      // config: './tailwind.config.ts'
    },
    autoprefixer: {},
  },
};
